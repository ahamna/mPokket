server:
  port: 4444
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/contactlist?useSSL=false
    username: contactlist_user
    password: contactlist_password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
